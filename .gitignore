# macOS / OS cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
Desktop.ini

# IDEs / editors
.idea/
.vscode/
*.iml
*.swp
*~

# Go build & test output
*.exe
*.exe~
*.test
*.out
*.prof
coverage.*
*.cover
# Module vendoring is done ephemerally in CI (and by the offline build); we do
# not commit it.
vendor/

# docker-slim output (produced by check.sh and the build pipeline)
slim.report.json
.slim-state/

# Offline image build artifacts (downloaded/unzipped vuln database, generated SBOM)
/vulndb.zip
/vulndb/
/sbom/
*.spdx.json
*.cdx.json

# Local environment & logs
.env
.env.*
!.env.example
*.log
